{
  "name": "neottia-monorepo",
  "private": true,
  "type": "module",
  "description": "Unifying AI harnesses",
  "author": "Dragos Cirjan",
  "license": "MIT",
  "packageManager": "pnpm@10.28.2",
  "pnpm": {
    "overrides": {
      "vite": ">=6.4.3",
      "zod": "^4.5.4",
      "vitest": "^5.0.0"
    }
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "audit": "pnpm audit --audit-level=moderate",
    "build": "pnpm -r run --if-present build",
    "changeset": "changeset",
    "clean": "pnpm -r run --if-present clean && rimraf .nyc_output coverage docs/.vitepress/dist docs/.vitepress/cache",
    "config:contracts": "node scripts/config-contracts.mjs",
    "config:schema:generate": "node scripts/config-contracts.mjs --write",
    "docs": "vitepress build docs",
    "docs:check": "vitest run --root . scripts/documentation.spec.ts",
    "docs:preview": "vitepress preview docs",
    "duplicate-check": "jscpd packages/ extensions/",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "license-check": "pnpm licenses list --prod",
    "lint": "eslint --fix .",
    "lint:check": "eslint .",
    "release:global": "pnpm --filter @neottia/release release:prepare",
    "release:modules": "changeset publish",
    "serve": "vitepress dev docs",
    "start": "pnpm --filter @neottia/core start",
    "start:debug": "pnpm --filter @neottia/core start:debug",
    "test": "node scripts/run-workspace-tests.mjs",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "typecheck": "pnpm -r run --if-present typecheck",
    "validate": "pnpm run format:check && pnpm run docs:check && pnpm run lint:check && pnpm run duplicate-check && pnpm run test && pnpm run build && pnpm run config:contracts && pnpm run docs",
    "version:modules": "changeset version"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.2",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@jscpd/badge-reporter": "^4.0.1",
    "@templ-project/eslint": "^1.3.0",
    "@templ-project/prettier": "^1.1.0",
    "@types/node": "^26.5.1",
    "@vitest/coverage-v8": "^5.0.0",
    "esbuild": "^0.28.2",
    "eslint": "^10.10.0",
    "husky": "^9.1.7",
    "import-sort-style-module": "^6.0.0",
    "jscpd": "^5.2.0",
    "lint-staged": "^16.2.7",
    "npm-run-all2": "^9.0.3",
    "prettier": "^3.9.6",
    "prettier-plugin-import-sort": "^0.0.7",
    "prettier-plugin-toml": "^2.0.6",
    "rimraf": "^6.1.2",
    "tsx": "^4.21.0",
    "typescript": "^7.0.2",
    "vitepress": "^1.6.4",
    "vitest": "^5.0.0",
    "yaml": "^2.9.0"
  }
}
