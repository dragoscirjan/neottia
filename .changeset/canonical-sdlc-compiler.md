---
"@neottia/sdlc": minor
"@neottia/distribution": patch
---

Compile the canonical six-command SDLC into deterministic Pi and OpenCode asset manifests with explicit provider, template, role, configuration, and runtime package provenance.

Export deterministic code-unit ordering from the distribution package for compiler and extension authors.
