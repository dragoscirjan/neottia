---
"@neottia/config": minor
---

Publish the complete strict root JSON Schema and expose registry-based schema generation for embedding applications.

Document unified source files, profiles, precedence, secret references, provenance, diagnostics, host policy, and migration from `skills.*` to `modules.*`.
