---
"@neottia/memory-core": minor
---

Initial release: standalone agent memory with filesystem-canonical YAML records, SQLite FTS5 (BM25) index, shard-scoped locking, supersession/tombstone lifecycle, secret scanning, and the sharded `skills.memory` config contract with `NEOTTIA_MEMORY_*` env overrides.
