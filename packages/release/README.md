# @neottia/release

The global Neottia release bill of materials. Each release pins the exact published version of every Neottia module.

This package is prepared with:

```bash
mise run release:global -- 1.0.0
```
