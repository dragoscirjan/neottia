# Memory MCP compatibility route

The Memory MCP guide moved to [the canonical Memory MCP page](/mcp/memory).

This route remains available for existing links. Tool contracts stay in the [Memory tool reference](/memory/tools), while client setup and server behavior stay in the MCP guide.

The filesystem backend ignores `namespace.scope`; separate worktrees use separate roots. Namespace scope separates PostgreSQL rows within one organization and project.
